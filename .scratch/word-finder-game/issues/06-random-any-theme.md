# 06: Random/Any theme

**What to build:** A "Random/Any" theme option, completing the full theme set from the spec. Word data comes from a dev-only, one-off Node script (not part of the deployed app) that reads the `word-list` npm package (~15,600 words, MIT licensed), filters to 3-14 letters, and writes a static `data/random-words.json` checked into the repo. The deployed site fetches that static JSON exactly like a curated theme file — no npm package, build step, or server-side code at runtime or deploy time. "Random/Any" is added to the start screen's theme selector as a seventh option, with word-count capping applying the same as any curated theme.

**Blocked by:** 05 (extends the theme selector and reuses the same data-loading path curated themes established)

**Status:** wontfix

- [x] A one-off, dev-only Node script generates `data/random-words.json` from the `word-list` npm package, filtered to 3-14 letters
- [x] The generation script is not invoked at deploy time or app runtime — it's a manual/dev step producing a checked-in static file
- [x] "Random/Any" appears as a seventh option in the start screen's theme selector
- [x] Selecting Random/Any and starting the puzzle draws words from `data/random-words.json`
- [x] Word-count capping applies correctly for Random/Any (given its own, lower 20x20 max in ticket 07's follow-up work — see `spec.md`'s Further Notes)
- [x] Puzzles generated from Random/Any play correctly end-to-end (placement, selection, win)
- [x] Deployed app still requires no build step and loads no npm packages at runtime

## Comments

> *This was generated by AI during triage.*

Already implemented and merged (commit `cf4e048`, "Merge ticket 06: Random/Any theme"), now on `master` via fast-forward merge `9a96876`. Closing as already-implemented, not a rejection.
